# REDLINE Web GUI - Python 3.9 Compatible Requirements
# Conservative versions that work with Python 3.9

# Web Framework (Flask GUI) - Python 3.9 compatible versions
Flask==2.3.3
Flask-Compress==1.13
Flask-SocketIO==5.3.6
gunicorn==21.2.0

# Core Data Processing - Python 3.9 compatible versions
pandas==2.0.3
numpy==1.24.3

# Financial Data Download - Python 3.9 compatible version
yfinance==0.2.18

# Data Storage - Python 3.9 compatible version
duckdb==0.8.1

# Network and HTTP - Python 3.9 compatible versions
requests==2.31.0
urllib3==2.0.7

# Configuration and File I/O - Python 3.9 compatible versions
configparser==5.3.0
werkzeug==2.3.7

# Data Processing Extensions - Python 3.9 compatible version
pyarrow==12.0.1

# Essential Utilities
setuptools==68.2.2
wheel==0.41.2

# Background Task Processing
celery==5.5.3
redis==6.4.0

# Cryptography and SSL support (CRITICAL - fixes _cffi_backend error)
cryptography==41.0.7
cffi==1.16.0
pycparser==2.21

# Additional dependencies that might be needed
python-socketio==5.3.6
pandas-datareader==0.10.0

# Optional but recommended for enhanced functionality (Python 3.9 compatible)
polars==0.20.31
feather-format==0.4.1
