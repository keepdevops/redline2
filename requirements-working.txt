# REDLINE Web GUI - Python 3.11 Compatible Requirements
# Same versions as main requirements.txt for full feature parity

# Core Data Processing - Python 3.11 compatible versions
pandas>=2.3.3
numpy>=2.3.4
yfinance>=0.2.66

# Data Processing and Storage - Python 3.11 compatible versions
pyarrow>=21.0.0
polars>=1.34.0
duckdb>=1.4.1

# Web Framework (Flask GUI) - Python 3.11 compatible versions
flask>=3.1.2
flask-socketio>=5.5.1
flask-compress>=1.20
gunicorn>=23.0.0

# Background Task Processing
celery>=5.5.3
redis>=6.4.0

# Network and HTTP - Python 3.11 compatible versions
requests>=2.31.0
urllib3>=2.0.0

# Configuration and Logging
configparser>=5.3.0

# Cryptography and SSL support (CRITICAL - fixes _cffi_backend error)
cryptography>=41.0.7
cffi>=2.0.0
pycparser>=2.22

# Additional dependencies
python-socketio>=5.5.1
pandas-datareader>=0.10.0
feather-format>=0.4.1

# Essential Utilities
setuptools>=65.0.0
wheel>=0.40.0

# Optional Dependencies for Enhanced Features
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Development Dependencies (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# File I/O and Utilities
openpyxl>=3.1.0  # For Excel support (future)
xlsxwriter>=3.1.0  # For Excel writing (future)

# Multi-Platform Dependencies
importlib-metadata>=6.0.0  # For Flask version detection
platformdirs>=4.0.0  # For cross-platform directory handling
