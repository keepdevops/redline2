# REDLINE Core Dependencies - Multi-Platform Compatible (Python 3.11+)
pandas>=2.3.3
numpy>=2.3.4
yfinance>=0.2.66

# Data Processing and Storage
pyarrow>=21.0.0
fastparquet>=2024.2.0
polars>=1.34.0
duckdb>=1.4.1

# GUI Framework (built into Python)
# tkinter - included with Python standard library

# Web Framework (Flask GUI) - Multi-Platform Compatible
# Updated for security vulnerabilities (Jan 2025) - using latest available versions
flask>=3.1.2
flask-socketio>=5.5.1
flask-compress>=1.20
flask-limiter>=3.8.0
flask-cors>=3.0.0
gunicorn>=23.0.0

# Flask Core Dependencies (CRITICAL - Flask won't work without these)
# Updated for security vulnerabilities (Jan 2025) - using latest available versions
werkzeug>=3.1.3
jinja2>=3.1.6
markupsafe>=3.0.3
click>=8.3.0
blinker>=1.9.0
itsdangerous>=2.2.0

# SocketIO Dependencies (CRITICAL - Flask-SocketIO won't work without these)
python-socketio>=5.5.0
eventlet>=0.33.3
gevent>=23.7.0
gevent-websocket>=0.10.1

# Background Task Processing
celery>=5.5.0
redis>=5.0.0

# Optional Dependencies for Enhanced Features
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Development Dependencies (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Network and HTTP
# Updated for security vulnerabilities (Jan 2025) - using latest available versions
requests>=2.32.5
urllib3>=2.5.0
h2>=4.3.0  # Fixes CVE-2025-57804 (HTTP/2 request splitting)
# Note: brotli CVE-2025-6176 - fix (1.2.0+) not yet available on PyPI (latest: 1.1.0)
# Monitor: https://pypi.org/project/brotli/ for security update
# Workaround: Consider disabling brotli compression if security is critical

# Configuration and Logging
configparser>=5.3.0

# File I/O and Utilities
openpyxl>=3.1.0  # For Excel support (future)
xlsxwriter>=3.1.0  # For Excel writing (future)

# Multi-Platform Dependencies
importlib-metadata>=7.0.0  # For Flask version detection
setuptools>=75.0.0  # For proper package management
wheel>=0.45.0  # For wheel support
platformdirs>=4.0.0  # For cross-platform directory handling

# Security: Update pip to fix CVE-2025-8869 (install with: pip install --upgrade pip>=25.3)
# Note: pip is not in requirements.txt as it's a system package, but ensure pip>=25.3

# Payment Processing
# Updated for security vulnerabilities (Jan 2025)
stripe>=13.2.0

# Cloud Storage (optional)
# Updated for security vulnerabilities (Jan 2025) - using latest available version
boto3>=1.40.72  # Latest available: 1.40.72 (fixes security issues)