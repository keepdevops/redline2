# REDLINE Core Dependencies - Multi-Platform Compatible (Python 3.11+)
pandas>=2.3.3
numpy>=2.3.5
yfinance==0.2.40  # Downgraded from 0.2.66 to fix chrome136 impersonation error

# Data Source Client Libraries (Optional - fallback to REST API if not installed)
massive>=2.0.1  # Massive.com client library (optional - falls back to REST API if not installed)

# Data Processing and Storage
pyarrow>=22.0.0  # Updated for security patches
fastparquet>=2024.11.0  # Updated for security patches
polars>=1.35.2  # Updated for security patches
duckdb>=1.4.2  # Updated for security patches

# GUI Framework (built into Python)
# tkinter - included with Python standard library

# Web Framework (Flask GUI) - Multi-Platform Compatible
# Updated for security vulnerabilities (Jan 2025) - using latest available versions
flask>=3.1.2
flask-socketio>=5.5.1
flask-compress>=1.23  # Updated for security patches
flask-limiter>=4.0.0  # Updated to latest major version - test compatibility
flask-cors>=6.0.1  # Updated for security patches
gunicorn>=23.0.0

# Flask Core Dependencies (CRITICAL - Flask won't work without these)
# Updated for security vulnerabilities (Jan 2025) - using latest available versions
werkzeug>=3.1.3
jinja2>=3.1.6
markupsafe>=3.0.3
click>=8.3.1
blinker>=1.9.0
itsdangerous>=2.2.0

# SocketIO Dependencies (CRITICAL - Flask-SocketIO won't work without these)
python-socketio>=5.14.3  # Updated for security patches
eventlet>=0.40.3  # Updated for security patches
gevent>=25.9.1  # Updated for security patches (major version - test compatibility)
gevent-websocket>=0.10.1

# Background Task Processing
celery>=5.5.3  # Updated for security patches
redis>=7.1.0  # Updated to latest version (7.1.0) - test compatibility before deploying

# Optional Dependencies for Enhanced Features
scikit-learn>=1.7.2  # Updated for security patches
matplotlib>=3.10.7  # Updated for security patches
seaborn>=0.13.2  # Updated for security patches

# ML Format Support (Optional - enables Keras .h5 and TensorFlow .npz formats)
tensorflow>=2.20.0  # Enables Keras and TensorFlow format support (updated from 2.18.0)

# Development Dependencies (optional)
pytest>=9.0.1  # Updated for security patches
black>=25.11.0  # Updated for security patches
flake8>=7.3.0  # Updated for security patches

# Network and HTTP
# Updated for security vulnerabilities (Jan 2025) - using latest available versions
requests>=2.32.5
urllib3>=2.5.0
curl-cffi>=0.7.0  # Required by yfinance for Yahoo Finance API access
h2>=4.3.0  # Fixes CVE-2025-57804 (HTTP/2 request splitting)
brotli>=1.2.0  # Updated - fixes CVE-2025-6176 (security update now available)

# Configuration and Logging
configparser>=7.2.0  # Updated for security patches

# File I/O and Utilities
openpyxl>=3.1.5  # Updated for security patches
xlsxwriter>=3.2.9  # Updated for security patches

# Multi-Platform Dependencies
importlib-metadata>=8.7.0  # Updated for security patches
setuptools>=80.9.0  # Updated for security patches
wheel>=0.45.1  # Updated for security patches
platformdirs>=4.5.0  # Updated for security patches

# Security: Update pip to fix CVE-2025-8869 (install with: pip install --upgrade pip>=25.3)
# Note: pip is not in requirements.txt as it's a system package, but ensure pip>=25.3

# Payment Processing
# Updated for security vulnerabilities (Jan 2025)
stripe>=14.0.1

# Cloud Storage (optional)
# Updated for security vulnerabilities (Jan 2025) - using latest available version
boto3>=1.41.2  # Updated: 1.41.2 (fixes security issues)
watchdog>=6.0.0  # File system monitoring for auto-copy functionality